# translator-plugs — Chrome 网页翻译插件

**中文** | [English](./README_EN.md)

> 使用大模型（DeepSeek / 智谱GLM / 火山引擎 / MiniMax / OpenAI 等）将英文网页翻译成中文，支持对照翻译、划词翻译、单词查询、扫描版 PDF 图片翻译。

---

## 目录

- [一、快速开始（安装教程）](#一快速开始安装教程)
- [二、功能按钮一览](#二功能按钮一览)
- [三、配置大模型（保姆级教程）](#三配置大模型保姆级教程)
- [四、核心功能详解](#四核心功能详解)
- [五、使用场景示例](#五使用场景示例)
- [六、项目架构](#六项目架构)
- [七、特色功能深入讲解](#七特色功能深入讲解)
- [八、注意事项与常见问题](#八注意事项与常见问题)

---

## 一、快速开始（安装教程）

### 1.1 什么是 Chrome 插件？

Chrome 插件是运行在 Chrome 浏览器中的小程序，可以增强浏览器的功能。本插件的作用是：**在你浏览任何网页时，右侧会出现一个翻译浮动面板，点击即可翻译。**

### 1.2 安装步骤

**前提条件**：你需要一个 Chrome 内核浏览器（Chrome、Edge、360极速浏览器、Brave 等均可）。

1. **下载项目文件**

   将整个 `translator-plugs` 文件夹下载到本地任意位置，例如 `D:\source\translator-plugs`。

2. **打开浏览器的扩展管理页面**

   - Chrome：地址栏输入 `chrome://extensions/` 回车
   - Edge：地址栏输入 `edge://extensions/` 回车

3. **开启「开发者模式」**

   在扩展管理页面的右上角，找到「开发者模式」开关，打开它。

4. **加载已解压的扩展程序**

   点击左上角的「加载已解压的扩展程序」按钮，选择 `translator-plugs` 文件夹。

5. **安装成功**

   你会在扩展列表中看到「larf的翻译」。此时打开任意网页，页面右侧会出现翻译浮动面板。

> **提示**：如果修改了代码，需要在扩展管理页面点击「重新加载」按钮，然后刷新网页才能生效。

---

## 二、功能预览

![浮动面板（悬停展开状态）](screenshots/float-panel-hover.png)
*图：网页右侧浮动面板，鼠标悬停后展开所有功能按钮*

---

## 三、功能按钮一览

在网页右侧的浮动面板上，鼠标悬停后会展开以下按钮（从上到下）：

```
┌──────┐
│ 翻译 │  ← 蓝色大按钮，翻译整个页面（英文→中文）
├──────┤
│ 单词 │  ← 单词/句子翻译，手动输入要翻译的内容
├──────┤
│ 对照 │  ← 对照翻译，原文下方紧跟着译文
├──────┤
│ PIC  │  ← 图片/扫描版PDF翻译（需要多模态大模型）
├──────┤
│ ⚙️  │  ← 配置面板，管理大模型
└──────┘
```

**按钮状态说明**：

| 按钮状态 | 含义 |
|---------|------|
| 蓝色「翻译」 | 初始状态，点击开始翻译 |
| 绿色「原文」 | 翻译完成，点击恢复原文 |
| 灰色「翻译中」 | 正在翻译，请等待 |
| 绿色「对照中」 | 对照翻译进行中或已完成 |
| 红色「取消」 | PIC翻译进行中，点击可取消 |

---

## 三、配置大模型（保姆级教程）

> 插件本身不自带翻译能力，需要你配置一个大模型的 API。可以理解为：插件是"遥控器"，大模型是"发动机"，你需要自己提供发动机。

### 3.1 如何获取 API Key？

以 DeepSeek 为例（推荐新手使用，便宜且翻译质量好）：

1. 访问 [DeepSeek 开放平台](https://platform.deepseek.com/)
2. 注册账号并登录
3. 进入「API Keys」页面，点击「创建 API Key」
4. 复制生成的 Key（形如 `sk-xxxxxxxxxxxx`），**这个 Key 只显示一次，请妥善保存**

其他模型获取方式类似：
| 模型 | 获取地址 | 备注 |
|------|---------|------|
| DeepSeek | https://platform.deepseek.com/ | 推荐，性价比高 |
| 智谱 GLM | https://open.bigmodel.cn/ | 国产，支持多模态 |
| 火山引擎 | https://www.volcengine.com/ | 字节跳动旗下 |
| MiniMax | https://platform.minimax.chat/ | 支持多模态 |
| OpenAI | https://platform.openai.com/ | 需要海外网络 |

### 3.2 在插件中配置模型

1. **打开配置面板**：点击浮动面板上的 ⚙️ 齿轮图标

2. **添加模型**：点击「+ 添加模型」按钮

3. **填写模型信息**：

   ```
   快速选择：下拉选择预设模型（推荐，会自动填充下面三项）
   
   模型名称：显示用，随意填，如 "DeepSeek翻译"
   API 地址：https://api.deepseek.com/v1 （预设会自动填）
   API 密钥：sk-xxxxxxxxxxxxxxxxxx （粘贴你获取的 Key）
   模型标识：deepseek-v4-flash （预设会自动填）
   ```

4. **开关说明**：

   - **Thinking 模式**：开启后大模型会"深度思考"再翻译，质量略高但速度更慢。一般关闭即可。
   - **视觉（支持图片翻译）**：开启后该模型可用于 PIC 按钮翻译图片/PDF。只有支持多模态的模型才能开启（如 GLM-4V、GPT-4o 等）。

5. **保存**：点击「保存」按钮

6. **设为默认**：在模型列表中，点击「设为默认文本」按钮，将该模型设为翻译按钮的默认模型

7. **测试模型**：点击「测试」按钮，验证配置是否正确。如果返回翻译结果说明配置成功。

### 3.3 双默认模型机制

本插件有**两种默认模型**，可以设置为同一个模型，也可以设为不同的模型：

| 默认类型 | 用途 | 蓝色徽章标识 |
|---------|------|------------|
| 默认文本 | 「翻译」「对照」「单词」「划词」 | `文本` 蓝色 |
| 默认 PIC | 「PIC」按钮的图片/PDF翻译 | `PIC` 橙色 |

**为什么要分两种？** 多模态模型通常比纯文本模型慢很多。如果你用多模态模型翻译普通文本，速度会很慢。分开设置后，普通翻译用快速文本模型，图片翻译用多模态模型，各取所长。

> **提示**：只有开启了「视觉」开关的模型才能设为 PIC 默认。

---

## 四、核心功能详解

### 4.1 页面翻译（「翻译」按钮）

**用途**：将整个网页的英文内容替换为中文。

**工作流程**：
1. 点击「翻译」按钮
2. 插件自动收集网页中所有英文文本节点
3. 智能过滤：跳过代码块、JSON、纯数字、UI按钮文字（如 "OK"、"Login"）
4. 分批发送给大模型翻译（每批30条，3条并发）
5. 将翻译结果替换原文，按钮变绿显示「原文」
6. 再次点击恢复英文原文

**翻译方向**：
- 英文 → 中文（默认）
- 中文 → 英文（在扩展弹窗中切换）

### 4.2 对照翻译（「对照」按钮）

**用途**：不修改原文，在每段英文下方插入中文译文，方便对照阅读。

![对照翻译效果](screenshots/compare-translation.png)
*图：对照翻译模式，原文下方显示灰色译文卡片*

**特点**：
- 译文卡片有灰色左侧线，紧凑显示在原文下方
- 不破坏页面DOM结构，译文放在浮动层中
- 表格/列表中自动使用紧凑模式
- 支持取消（再次点击「对照」按钮）

### 4.3 单词翻译（「单词」按钮）

**用途**：手动输入单词或句子进行翻译，不依赖页面内容。

**使用方法**：
1. 点击「单词」按钮打开输入面板
2. 在输入框中输入要翻译的内容
3. 按回车或点击翻译按钮
4. 译文显示在下方

### 4.4 划词翻译

**用途**：选中网页中的文字，自动弹出翻译按钮。

**使用方法**：
1. 用鼠标选中网页中的一段文字
2. 选中文字附近会弹出一个小「翻译」按钮
3. 点击按钮即可翻译选中的文字

**开关**：在扩展弹窗（点击浏览器工具栏的插件图标）中可以开关划词翻译功能。

### 4.5 PIC 图片/PDF 翻译（「PIC」按钮）

**用途**：翻译扫描版 PDF 或网页中的图片内容。这是本插件的特色功能。

**适用场景**：
- 在线扫描版 PDF（如合同、报告、扫描文档）
- react-pdf 渲染的 Canvas 页面
- 页面中的图片（未来扩展）

**工作原理**：
1. 检测页面中的 Canvas 元素（PDF 渲染页面）
2. 判断是否为扫描版（检查 textLayer 是否为空）
3. 将每页 Canvas 转为 JPEG 图片
4. 发送给多模态大模型（如 GLM-4V、GPT-4o）进行 OCR + 翻译
5. 在每页 PDF 下方显示译文卡片（支持 Markdown 渲染）
6. 右上角显示醒目的进度面板（百分比 + 当前页/总页数）

![PIC翻译进度面板](screenshots/pic-progress.png)
*图：PIC翻译进度面板，实时显示百分比和当前页状态*

**使用前提**：
- 必须配置一个开启了「视觉」开关的大模型
- 必须将该模型设为「PIC 默认」或任意多模态模型（自动回退）

---

## 五、使用场景示例

### 场景 1：阅读英文技术文档

1. 打开一个英文文档页面（如 React 官方文档）
2. 点击「翻译」→ 整个页面变中文
3. 阅读完毕点击「原文」→ 恢复英文

### 场景 2：对照阅读长篇文章

1. 打开英文文章
2. 点击「对照」→ 每段英文下方出现中文译文
3. 对照阅读，理解更准确
4. 阅读完毕点击「对照」→ 移除译文

### 场景 3：翻译扫描版 PDF 合同

1. 打开在线 PDF（如 WorldFirst 的合同页面）
2. 确保已配置多模态模型（如 GLM-4V）并设为 PIC 默认
3. 点击「PIC」按钮
4. 右上角出现进度面板，显示"正在翻译第 N 页..."
5. 翻译完成后，每页 PDF 下方出现译文卡片
6. 如需取消，再次点击「取消」按钮

### 场景 4：快速翻译不认识的单词

1. 双击选中网页中的单词（或鼠标拖选）
2. 弹出翻译按钮，点击翻译
3. 或者点击「单词」按钮，手动输入翻译

### 场景 5：中文翻译成英文

1. 点击浏览器工具栏的插件图标
2. 在「翻译语言」下拉框中选择「中文 → 英文」
3. 点击「确认」保存
4. 打开中文网页，点击「翻译」→ 变成英文

---

## 六、项目架构

### 6.1 文件结构

```
translator-plugs/
├── manifest.json      ← 插件配置文件（MV3 格式）
├── content.js         ← 内容脚本（核心逻辑，注入到网页中）
├── background.js      ← 后台服务工作者（处理 API 请求）
├── popup.html         ← 扩展弹窗页面（点击工具栏图标弹出）
├── popup.js           ← 弹窗交互逻辑
├── style.css          ← 注入网页的样式文件
├── icon16.png         ← 工具栏图标（16x16）
├── icon48.png         ← 管理页面图标（48x48）
├── icon128.png        ← 商店图标（128x128）
└── test/              ← 测试文件目录
```

### 6.2 技术栈

| 技术 | 用途 |
|------|------|
| Chrome Extension MV3 | 插件架构标准 |
| 原生 JavaScript | 无框架依赖，纯 JS 实现 |
| Chrome Storage API | 持久化存储模型配置 |
| Chrome Messaging API | content ↔ background 通信 |
| OpenAI 兼容 API | 大模型调用接口（兼容所有 OpenAI 格式的 API） |

### 6.3 架构图

```
┌─────────────────────────────────────────────────┐
│                   网页 (Web Page)                │
│                                                  │
│  ┌─────────────┐     ┌──────────────────────┐  │
│  │  content.js │     │     style.css         │  │
│  │             │     │  (注入的样式)          │  │
│  │ · 浮动面板   │     │  · 按钮样式            │  │
│  │ · 配置面板   │     │  · 翻译卡片样式        │  │
│  │ · 翻译逻辑   │     │  · 弹窗样式            │  │
│  │ · 文本收集   │     └──────────────────────┘  │
│  │ · DOM 操作   │                               │
│  └──────┬──────┘                               │
│         │ chrome.runtime.sendMessage            │
└─────────┼───────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────┐
│              background.js (Service Worker)      │
│                                                  │
│  · 接收翻译请求                                   │
│  · 调用大模型 API (fetch)                         │
│  · 超时控制 (120秒)                               │
│  · 返回翻译结果                                   │
│  · 支持: translateText / translateMultimodal     │
└─────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────┐
│              popup.html / popup.js               │
│           (扩展弹窗 - 独立页面)                    │
│                                                  │
│  · 翻译方向切换 (英→中 / 中→英)                   │
│  · 超时时间设置                                    │
│  · 划词翻译开关                                    │
│  · 大模型配置 (添加/编辑/删除/测试)                 │
└─────────────────────────────────────────────────┘
```

### 6.4 数据流

**页面翻译流程**：

```
用户点击「翻译」
    ↓
content.js 收集网页文本节点
    ↓
过滤（跳过代码、中文、UI文字等）
    ↓
分批（每批30条，3条并发）
    ↓
chrome.runtime.sendMessage → background.js
    ↓
background.js 调用大模型 API
    ↓
返回翻译结果 → content.js
    ↓
替换原文 / 插入译文卡片
```

**PIC 翻译流程**：

```
用户点击「PIC」
    ↓
content.js 检测页面 Canvas (PDF页面)
    ↓
判断是否扫描版 (textLayer 为空)
    ↓
逐页: Canvas → JPEG Base64
    ↓
chrome.runtime.sendMessage → background.js
    ↓
background.js 调用多模态 API (Vision)
    ↓
返回译文 → content.js
    ↓
在 Canvas 下方插入译文卡片 (Markdown渲染)
    ↓
更新进度面板 (百分比 + 当前页)
```

---

## 七、特色功能深入讲解

### 7.1 智能文本过滤

插件不会盲目翻译所有文字，而是智能过滤：

| 过滤类型 | 示例 | 原因 |
|---------|------|------|
| 代码块 | `function foo() {}` | 代码不应翻译 |
| JSON 数据 | `{"name": "test"}` | 数据结构不应翻译 |
| 纯数字/日期 | `2024-01-01` | 无需翻译 |
| 已是中文 | `你好世界` | 中文内容跳过 |
| UI按钮文字 | `OK`, `Login`, `Submit` | 常见UI文字不翻译 |
| GitHub错误弹窗 | `Something went wrong` | 隐藏模板，不需要翻译 |
| 版本号 | `v1.2.3` | 无需翻译 |

### 7.2 Markdown 译文渲染

PIC 翻译的译文卡片支持 Markdown 渲染，大模型返回的翻译会自动格式化：

| Markdown 语法 | 渲染效果 |
|--------------|---------|
| `# 标题` | 大标题 |
| `**加粗**` | **加粗** |
| `*斜体*` | *斜体* |
| `- 列表项` | 项目符号列表 |
| `1. 有序列表` | 数字列表 |
| `\| 表格 \|` | 表格 |
| `` `代码` `` | 代码片段 |
| `> 引用` | 引用块 |

### 7.3 浮动面板防丢失

网页可能因为 React 渲染、页面脚本等原因删除插件注入的 DOM 元素。插件内置了 `watchPanelRemoval` 机制：

- 每 2 秒检查一次浮动面板是否还存在
- 如果被删除，自动重新创建所有 UI 元素
- 恢复按钮状态（翻译中、对照中等）

### 7.4 双默认模型

如前所述，支持「默认文本」和「默认PIC」两种默认模型设置：

- **文本翻译**：使用快速文本模型（如 DeepSeek Flash），响应快
- **PIC翻译**：使用多模态模型（如 GLM-4V），能识别图片
- 两类默认互不影响，可设为同一模型
- 点击已默认的按钮可取消默认（toggle 逻辑）

### 7.5 批量并发翻译

为提高翻译速度，插件采用批量并发策略：

- **批量大小**：每批 30 条文本
- **并发数**：3 条同时发送
- **超时保护**：每条最多等待 60 秒（可配置 30-600 秒）
- **失败容错**：单条失败不影响其他条目

### 7.6 跨 iframe 支持

部分网页使用 iframe 嵌套内容（如 GitHub 的文件查看器）。插件在所有 iframe 中运行，并通过 `postMessage` 实现跨框架翻译协调。

---

## 八、注意事项与常见问题

### 8.1 重要注意事项

1. **API 费用**：大模型 API 调用会产生费用，费用由你使用的 API 提供商收取，与本插件无关。建议关注你的 API 余额。

2. **网络代理**：如果你使用国外的 API（如 OpenAI），需要科学上网。本插件不包含任何代理功能。

3. **API Key 安全**：你的 API Key 存储在 Chrome 本地存储中，不会上传到任何服务器。但请不要在公共电脑上配置。

4. **翻译质量**：翻译质量取决于你选择的大模型，本插件只负责调用 API 和展示结果。

5. **重新加载**：修改代码或更新配置后，需要在 `chrome://extensions/` 点击「重新加载」，然后刷新网页。

### 8.2 常见问题

**Q: 点击「翻译」后没反应？**

A: 检查以下几点：
1. 是否已配置大模型？点击 ⚙️ 查看模型列表
2. 是否已设为「默认文本」？
3. 点击「测试」按钮看是否能正常返回
4. 检查 API Key 是否正确、API 余额是否充足

**Q: 翻译速度很慢？**

A: 可能原因：
1. 使用了 Thinking 模式 → 关闭它
2. 使用了多模态模型翻译普通文本 → 将快速文本模型设为「默认文本」
3. 网页内容太多 → 正常现象，等待即可
4. API 服务端响应慢 → 换一个更快的模型

**Q: PIC 按钮提示"您的大模型不支持多模态"？**

A: PIC 翻译需要多模态模型。请在配置面板中：
1. 添加一个支持多模态的模型（如 GLM-4V、GPT-4o）
2. 开启「视觉」开关
3. 测试通过后设为「PIC 默认」

**Q: PIC 翻译时页面卡住不动？**

A: 多模态模型处理图片较慢（每页可能需要 30-120 秒）。请看右上角的进度面板，会显示"正在翻译第 N 页..."。如果确实卡住了，点击红色「取消」按钮。

**Q: 对照翻译的译文位置不对？**

A: 译文卡片使用浮动层定位，极少数网页的特殊布局可能导致位置偏移。这是已知限制，尝试滚动页面或重新翻译。

**Q: 划词翻译不生效？**

A: 在扩展弹窗中检查「划词翻译」开关是否打开。部分网页可能阻止选中文本。

**Q: 翻译后页面排版乱了？**

A: 某些网页的 CSS 与翻译后的中文文字长度不匹配可能导致排版变化。这是网页本身的限制，尝试使用「对照翻译」代替直接替换。

**Q: 如何切换翻译方向（英→中 / 中→英）？**

A: 点击浏览器工具栏的插件图标，在「翻译语言」下拉框中切换，点击「确认」保存。

### 8.3 已知限制

1. **不支持本地 PDF 文件**：PIC 翻译仅支持在线 PDF 页面（通过 Canvas 渲染的）
2. **不支持视频字幕翻译**：当前版本不处理视频中的字幕
3. **动态内容需重新翻译**：网页动态加载新内容后，需要再次点击翻译
4. **超长文本截断**：单条文本超过 50000 字符会被跳过

---

## 九、开发指南

### 9.1 开发环境

- 任何文本编辑器（VS Code 推荐）
- Chrome 内核浏览器
- 不需要 Node.js 或构建工具（纯原生 JS）

### 9.2 代码结构概览

#### content.js 主要函数

| 函数名 | 行号约 | 功能 |
|--------|--------|------|
| `init()` | 52 | 初始化，创建UI，加载配置 |
| `handleTranslateClick()` | 2581 | 「翻译」按钮点击处理 |
| `handleCompareClick()` | 120 | 「对照」按钮点击处理 |
| `handlePicClick()` | 136 | 「PIC」按钮点击处理 |
| `startCompareTranslation()` | 205 | 对照翻译主逻辑 |
| `translateScannedPdf()` | 839 | 扫描PDF翻译主逻辑 |
| `detectScannedPdf()` | 1276 | 检测扫描版PDF |
| `shouldSkipTranslation()` | 341 | 智能文本过滤 |
| `renderMarkdown()` | 616 | Markdown渲染 |
| `createFloatPanel()` | 1393 | 创建浮动面板 |
| `createConfigPanel()` | 1543 | 创建配置面板 |
| `renderModelList()` | 2108 | 渲染模型列表 |
| `handleModelAction()` | 2164 | 模型操作（默认/编辑/删除） |

#### background.js 主要函数

| 函数名 | 功能 |
|--------|------|
| `handleTranslateText(data)` | 文本翻译请求 |
| `handleTranslateMultimodal(data)` | 多模态翻译请求 |
| `buildTranslatePrompt(direction)` | 构建翻译提示词 |
| `buildMultimodalPrompt(direction)` | 构建OCR+翻译提示词 |
| `normalizeApiUrl(url)` | API地址标准化 |
| `buildNoThinkingParams(model)` | 构建关闭Thinking的参数 |

### 9.3 添加新的预设模型

在 `content.js` 和 `popup.js` 中的 `MODEL_PRESETS` 对象添加：

```javascript
const MODEL_PRESETS = {
  // ... 现有模型
  'your-model-id': {
    name: '显示名称',
    apiUrl: 'https://api.example.com/v1',
    model: 'model-id'
  }
};
```

同时在 `createAddModelModal()` 和 `popup.html` 的下拉框中添加对应的 `<option>`。

### 9.4 修改翻译行为

翻译提示词在 `background.js` 的 `buildTranslatePrompt()` 函数中，可以调整翻译风格、格式等。

### 9.5 调试技巧

1. **content.js 调试**：在网页上按 F12 打开开发者工具，在 Console 中查看日志
2. **background.js 调试**：在 `chrome://extensions/` 页面点击「Service Worker」链接
3. **popup.js 调试**：右键点击插件弹窗 → 「检查」
4. **存储数据查看**：开发者工具 → Application → Storage → Local Storage

### 9.6 代码规范

- 使用 `var` 声明变量（兼容性优先）
- 函数添加注释说明用途
- CSS 使用 `!important` 防止页面样式污染
- 全局状态变量集中管理（如 `isTranslating`、`isCompareMode`）

---

## 附录：预设模型列表

| 预设名 | 模型 | API 地址 | 支持多模态 |
|--------|------|---------|-----------|
| DeepSeek V4 Flash | deepseek-v4-flash | https://api.deepseek.com/v1 | 否 |
| DeepSeek V4 Pro | deepseek-v4-pro | https://api.deepseek.com/v1 | 否 |
| 智谱 GLM-5.1 | glm-5.1 | https://open.bigmodel.cn/api/paas/v4 | 否 |
| 火山 ark-code | ark-code-latest | https://ark.cn-beijing.volces.com/api/coding/v3 | 否 |
| MiniMax M2.7 | MiniMax-M2.7 | https://api.minimax.chat/v1 | 否 |

> **注意**：预设模型列表可能随厂商更新而变化，请以各厂商最新文档为准。如需使用多模态模型（用于PIC翻译），请手动添加并开启「视觉」开关。

---

*本项目仅供学习和个人使用，翻译质量取决于所选大模型，请合理使用 API 额度。*
